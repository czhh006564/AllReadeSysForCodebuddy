# “薪火传 书香伴‘共读一本书’公益爱心活动”
# 数字化系统 V1.0 云函数及业务接口设计说明书

**版本：V1.0**  
**适用技术：微信原生小程序 + 微信云开发 CloudBase**

---

# 一、文档目标

明确云函数及业务接口的职责边界、调用角色、请求参数、返回数据、权限校验、事务范围、幂等策略、状态变化、异常处理、错误码和审计要求。

核心原则：

> **前端负责展示和交互，云函数负责业务规则。**

---

# 二、总体技术原则

核心业务统一通过云函数。

必须经过云函数的操作包括：

- 身份认领；
- 身份解绑；
- 图书扫码判断；
- 开启阅读；
- 验书；
- 阅读关系建立；
- 分享互动；
- 教师点评/推荐；
- 荣誉变化；
- 图书投放回收；
- 未交处理；
- 异常扫码；
- 数据纠错；
- 载体更换；
- 统计。

---

# 三、云函数分域设计

推荐：

```text
auth
student
teacher
book
reading
social
honor
logistics
school
volunteer
message
content
statistics
admin
system
```

---

# 四、统一请求结构

```json
{
  "request_id": "唯一请求ID",
  "action": "业务动作",
  "data": {},
  "client_context": {
    "app_version": "1.0.0",
    "device": "wechat-mini-program"
  }
}
```

---

# 五、统一返回结构

成功：

```json
{
  "success": true,
  "code": "OK",
  "message": "操作成功",
  "data": {},
  "request_id": "xxx"
}
```

失败：

```json
{
  "success": false,
  "code": "BOOK_ALREADY_IN_READING",
  "message": "这本书当前已经有小读者正在阅读",
  "data": {},
  "request_id": "xxx"
}
```

---

# 六、统一幂等规则

关键写操作必须携带request_id，重复提交返回第一次成功结果，不再次执行。

---

# 七、统一身份上下文

云函数不能相信前端传入的student_id、teacher_id、volunteer_id、admin_id权限真实性，必须通过OPENID和绑定关系重新确认。

---

# 八、权限校验流程

```text
获取OPENID
↓
读取users
↓
识别当前角色
↓
验证角色权限
↓
验证数据范围
↓
执行业务
```

---

# 九、事务使用原则

必须事务处理：

- 开启阅读；
- 图书当前关系；
- 荣誉加减；
- 点赞评论及积分；
- 教师推荐及取消；
- 回收；
- 未交补回；
- 关键纠错。

---

# 十、auth.initSession

初始化用户身份，返回可用角色、当前角色、当前学生和是否需认领身份。

---

# 十一、auth.switchRole

切换角色或学生身份，必须验证当前微信确实绑定目标身份。

---

# 十二、student.getClaimContext

扫描班级二维码后返回学校、年级、班级和最小化学生列表。

---

# 十三、student.claimIdentity

校验学生存在、属于班级、状态有效、绑定冲突等，事务创建`student_wechat_bindings`并更新学生认领状态。

---

# 十四、student.listBoundStudents

返回当前微信已绑定学生列表。

---

# 十五、student.flagIdentityAbnormal

教师标记学生身份异常，教师不能直接解绑。

---

# 十六、student.getHome

聚合返回学生首页所需：

```text
student
current_reading
current_round
recent_messages
recent_books
honor_summary
```

---

# 十七、student.getReadingProfile

返回学生长期阅读档案，按角色和数据范围授权。

---

# 十八、book.scan

关键读取接口。

scene_code：

```text
NORMAL_CAN_START
OWN_ACTIVE_READING
OCCUPIED_BY_OTHER
BOOK_ABNORMAL
LOGISTICS_STATE_MISMATCH
INVALID_QR
```

---

# 十九、book.getDigitalProfile

返回永久图书数字生命页，学生端只能拿匿名历史和已授权内容。

---

# 二十、book.submitCheck

提交图书检查结果，扫码不等于建立阅读关系。

---

# 二十一、book.reportProblem

阅读中上报图书问题和换书申请，写入`book_abnormal_records`。

---

# 二十二、book.getReplacementRequest

学生查询换书处理状态。

---

# 二十三、reading.start

最高风险写接口之一。

事务：

```text
锁定book_instance
↓
确认未被占用
↓
reader_sequence = reader_count + 1
↓
创建reading_relation
↓
更新当前阅读关系
↓
更新reader_count
↓
更新round和投放识别统计
↓
生成开启阅读荣誉流水
↓
提交
```

返回reading_relation_id、reader_sequence、reading_type。

---

# 二十四、reading.getCurrent

返回当前阅读，无则`has_current_reading=false`。

---

# 二十五、reading.getDetail

返回单次阅读详情。

---

# 二十六、reading.changeCoverPhoto

只有当前active阅读学生可更换主题照片，旧照片保留历史。

---

# 二十七、reading.completeIndependent

仅自主阅读可主动完成，不改变实体物流归属。

---

# 二十八、reading.getHistory

分页获取历史书架。

---

# 二十九、reading.getMemorialCard

返回纪念卡所需结构化数据。

---

# 三十、reading.generateMemorialImage

生成署名版或匿名版纪念图。

---

# 三十一、reading.createPost

校验阅读关系、内容、图片数量、内容安全和班级审核模式；创建分享并生成POST_CREATE荣誉流水。

---

# 三十二、reading.editPost

允许学生编辑自己的历史分享，需重新内容安全检测。

---

# 三十三、reading.deletePost

逻辑删除，生成荣誉反向流水，不物理删除历史互动。

---

# 三十四、reading.getClassFeed

支持all / hot / teacher_recommended。

---

# 三十五、reading.getIndependentFeed

返回“同学们还在读”。

---

# 三十六、social.like

事务完成点赞、计数、荣誉、消息，必须幂等。

---

# 三十七、social.unlike

取消点赞并生成对应反向荣誉流水。

---

# 三十八、social.comment

创建一级评论并处理评论者和作者荣誉及消息。

---

# 三十九、social.deleteComment

允许评论作者、分享作者、教师、后台按权限删除并反向处理荣誉。

---

# 四十、teacher.getHome

返回教师首页聚合数据。

---

# 四十一、teacher.getClassOverview

验证教师班级关系后返回班级概览。

---

# 四十二、teacher.getStudentList

支持all / unclaimed / unchecked / reading筛选。

---

# 四十三、teacher.reviewPost

教师点评自己负责班级内容，不产生荣誉。

---

# 四十四、teacher.recommendPost

事务创建推荐、增加内容权重、学生荣誉、轮次统计和消息。

---

# 四十五、teacher.cancelRecommendation

推荐失效、权重回退、荣誉反向流水、统计调整。

---

# 四十六、teacher.moderatePost

支持hide / restore / approve / reject，写内容治理日志。

---

# 四十七、teacher.getAccompanimentArchive

返回教师历史陪伴记录，不做排名。

---

# 四十八、logistics.createInbound

志愿者/运营创建入仓记录，首次实体书建档后生成永久ID。

---

# 四十九、logistics.createDistribution

输入仓库、学校和投放明细，明细只包含年级、班级、书目、数量，不传book_uid数组。

事务创建投放单、投放明细、共读轮次和必要统计。

---

# 五十、logistics.getPendingRecoveries

返回待回收班级。

---

# 五十一、logistics.confirmBatchRecovery

数量一致时批量结束正常阅读关系，清理book_instance当前关系，更新回收和轮次。

---

# 五十二、logistics.confirmKnownMissingRecovery

数量不足但知道未交学生时，正常学生结束，未交学生保持active和missing，round=pending_close。

---

# 五十三、logistics.startDifferenceScan

数量不足且不知道缺哪本时创建异常扫码会话。

技术建议新增：

```text
recovery_scan_sessions
```

---

# 五十四、logistics.scanRecoveredBook

扫描已回收书，重复扫码不计数。

---

# 五十五、logistics.finishDifferenceScan

比较已识别book_uids和已扫描回收book_uids，返回缺失永久图书及当前学生。

---

# 五十六、logistics.confirmMissingBook

确认缺失后标记return_status=missing。

---

# 五十七、logistics.confirmLateReturn

补交后结束阅读关系并判断轮次是否完成。

---

# 五十八、logistics.getDistributionHistory

查询投放历史。

---

# 五十九、honor.applyEvent

内部业务服务，不建议前端直接调用。

---

# 六十、honor.rollbackEvent

根据原流水生成反向流水，不删除原流水。

---

# 六十一、honor.getStudentSummary

返回荣誉总值、等级、最高等级、下一等级、特殊荣誉。

---

# 六十二、honor.getLedger

分页读取当前学生荣誉流水。

---

# 六十三、honor.adminAdjust

后台人工调整必须创建ADMIN_ADJUST流水并写操作日志。

---

# 六十四、message.list

分页读取角色消息。

---

# 六十五、message.markRead

单条或批量已读。

---

# 六十六、content.getActivityHome

返回活动动态、已授权优秀分享和角色相关快捷数据。

---

# 六十七、content.authorize

后台记录线下已取得授权结果。

---

# 六十八、content.revokeAuthorization

撤回后取消扩大展示，不删除班级历史。

---

# 六十九、content.publishNews

后台发布文字+图片活动动态。

---

# 七十、school.getDashboard

返回当前学校数据驾驶舱。

---

# 七十一、school.getClassList

返回本校班级阅读状态。

---

# 七十二、school.getStudentProfile

学校管理员只读查看本校学生阅读档案。

---

# 七十三、volunteer.getWorkBench

返回待投放、待回收、今日操作、异常处理。

---

# 七十四、volunteer.getServiceArchive

返回志愿者服务记录，不做排名。

---

# 七十五、statistics.getOverview

支持时间、区域、学校、班级、书目、批次、公益来源等筛选。

---

# 七十六、statistics核心指标

至少包括：

```text
school_count
class_count
unique_student_count
reading_instance_count
distribution_quantity
recognized_book_quantity
reading_started_quantity
recovery_quantity
completed_circulation_quantity
post_count
like_count
comment_count
teacher_review_count
teacher_recommend_count
```

---

# 七十七、statistics.createSnapshot

生成锁定的公益成果快照。

---

# 七十八、admin.importPreview

Excel先校验再导入。

---

# 七十九、admin.confirmImport

正式导入并记录批次和错误明细。

---

# 八十、admin.correctStudent

后台学生资料纠错必须记录before/after/reason。

---

# 八十一、admin.revokeStudentBinding

错误认领使用status=revoked，不删除历史。

---

# 八十二、admin.correctBook

永久book_uid原则上不允许更改。

---

# 八十三、admin.replaceBookCarrier

记录物理载体更换，不改变book_uid、reader_count、公益来源、历史阅读。

---

# 八十四、admin.handleBookProblem

客服处理换书申请并生成学生消息，不建设换书物流。

---

# 八十五、admin.grantSpecialHonor

后台授予特殊荣誉并可生成证书。

---

# 八十六、system.getConfig

获取公开配置。

---

# 八十七、system.updateConfig

仅系统管理员可修改，写操作日志。

---

# 八十八、错误码设计

身份类：

```text
AUTH_REQUIRED
ROLE_NOT_ALLOWED
STUDENT_NOT_BOUND
STUDENT_NOT_FOUND
IDENTITY_ALREADY_BOUND
IDENTITY_CONFLICT
CLASS_TOKEN_INVALID
```

图书类：

```text
BOOK_NOT_FOUND
BOOK_QR_INVALID
BOOK_ABNORMAL
BOOK_ALREADY_IN_READING
BOOK_STATE_MISMATCH
BOOK_NOT_AVAILABLE
```

阅读类：

```text
READING_NOT_FOUND
READING_ALREADY_ACTIVE
READING_ALREADY_COMPLETED
BOOK_CHECK_REQUIRED
COVER_PHOTO_REQUIRED
READING_NOT_EDITABLE
```

内容类：

```text
POST_NOT_FOUND
POST_NO_PERMISSION
POST_CONTENT_EMPTY
POST_IMAGE_LIMIT_EXCEEDED
CONTENT_SECURITY_REJECTED
COMMENT_NOT_FOUND
```

教师类：

```text
TEACHER_CLASS_FORBIDDEN
POST_ALREADY_RECOMMENDED
POST_NOT_RECOMMENDED
```

物流类：

```text
ROUND_NOT_FOUND
ROUND_ALREADY_COMPLETED
RECOVERY_QUANTITY_INVALID
MISSING_STUDENT_INVALID
SCAN_BOOK_NOT_IN_ROUND
SCAN_BOOK_ALREADY_SCANNED
```

荣誉类：

```text
HONOR_RULE_NOT_FOUND
HONOR_EVENT_ALREADY_APPLIED
HONOR_LEDGER_NOT_FOUND
```

系统类：

```text
REQUEST_DUPLICATED
INVALID_PARAMETER
DATA_CONFLICT
TRANSACTION_FAILED
SYSTEM_BUSY
```

---

# 八十九、用户提示与错误码分离

前端展示自然语言，不直接显示技术错误码。

---

# 九十、接口安全设计

前端禁止直接写：

```text
reading_relations
book_instances
honor_ledger
teacher_recommendations
distribution_items
recovery_items
operation_logs
```

---

# 九十一、内容安全检查

分享、评论、教师点评、活动动态和图片应接入内容安全能力。

---

# 九十二、图片上传

云存储上传成功后，业务接口再次验证文件合法性和内容状态。

---

# 九十三、日志设计

建议记录：

```text
request_id
function_name
user_id
role
result_code
duration
created_at
```

高风险操作另写operation_logs。

---

# 九十四、操作审计范围

身份解绑、学生修正、永久图书维护、载体更换、投放撤销、回收修正、荣誉人工调整、特殊荣誉、内容隐藏、内容授权、系统配置变化。

---

# 九十五、接口数据最小化

不同角色即使调用同一业务接口，也应返回不同字段。

---

# 九十六、核心事务：开启阅读

必须全事务执行，任何一步失败全部回滚。

---

# 九十七、核心事务：点赞

点赞、计数、荣誉、消息统一事务。

---

# 九十八、核心事务：教师推荐

权限、推荐记录、权重、荣誉、消息、统计统一事务。

---

# 九十九、核心事务：正常回收

批量结束阅读关系，不要求工作人员逐本操作。

---

# 一百、核心事务：未交补回

补交后结束关系、更新图书和轮次状态。

---

# 一百零一、取消业务行为与荣誉反向流水

原流水永不删除，新增反向流水。

---

# 一百零二、分页规范

统一page / page_size；大规模后期可转cursor分页。

---

# 一百零三、排序规范

内容、消息、荣誉、历史书架按业务需要统一排序。

---

# 一百零四、接口版本控制

统一api_version=v1。

---

# 一百零五、推荐云函数实际拆分

```text
authService
studentService
teacherService
bookService
readingService
socialService
honorService
logisticsService
messageService
contentService
schoolService
volunteerService
statisticsService
adminService
systemService
```

---

# 一百零六、readingService示例

通过action路由startReading、getCurrent、getDetail、createPost、editPost、deletePost、getClassFeed、getHistory、getMemorialCard等。

---

# 一百零七、独立云函数建议

高并发或高风险操作可独立拆：

- startReading；
- confirmRecovery；
- generateMemorialImage；
- importData；
- statisticsAggregate。

---

# 一百零八、禁止超大万能云函数

不建议一个mainCloudFunction承载全部业务。

---

# 一百零九、前后端职责边界

前端负责展示、表单、loading、图片、跳转、友好提示。

云函数负责身份、权限、业务状态、唯一性、并发、荣誉、数据一致性和审计。

---

# 一百一十、前端不得自行判断的核心结果

例如“第X位小读者”、能否开启阅读、是否属于班级共读、荣誉分值、轮次是否完成等必须由云端返回。

---

# 一百一十一、缓存策略

可缓存书目基础信息、系统配置、区域、快捷点评；不长时间缓存当前阅读、图书占用、荣誉余额、回收状态。

---

# 一百一十二、性能设计

高频首页采用聚合接口，避免大量串行请求。

---

# 一百一十三、数据统计

实时事务只更新必要计数器，复杂统计按需聚合。

---

# 一百一十四、接口测试等级

每个接口至少做正常、权限、状态、重复、并发、回滚测试。

---

# 一百一十五—一百一十八、关键并发与重复测试

必须测试：

- 两学生同时开启一本书；
- 点赞重复；
- 教师推荐重复；
- 回收重复提交。

---

# 一百一十九、接口上线优先级

P0：

```text
auth.initSession
student.claimIdentity
student.listBoundStudents
student.getHome
book.scan
book.getDigitalProfile
reading.start
reading.getCurrent
reading.createPost
reading.editPost
reading.deletePost
reading.getClassFeed
social.like
social.unlike
social.comment
social.deleteComment
teacher.getHome
teacher.getClassOverview
teacher.reviewPost
teacher.recommendPost
teacher.cancelRecommendation
logistics.createDistribution
logistics.getPendingRecoveries
logistics.confirmBatchRecovery
logistics.confirmKnownMissingRecovery
logistics.scanRecoveredBook
logistics.confirmLateReturn
honor.getStudentSummary
honor.getLedger
message.list
admin基础数据接口
```

---

# 一百二十、P1

纪念图、内容治理、学校数据、志愿者档案、授权、活动动态、统计、快照、Excel、载体更换、特殊荣誉等。

---

# 一百二十一、V1.0明确不建设的接口

```text
box.*
replacement.delivery
replacement.shipping
replacement.sign
reading.checkIn
reading.progress
reading.exam
student.transferBook
student.giftBook
chat.*
volunteer.schedule
```

---

# 一百二十二、系统接口最终主链

身份 → 图书 → 阅读 → 互动 → 教师陪伴 → 回收 → 历史档案 → 公益统计。

---

# 一百二十三、后端开发核心判断标准

每个接口都必须回答：

1. 谁可调用；
2. 能操作哪些数据；
3. 当前状态是否允许；
4. 是否幂等；
5. 是否并发安全；
6. 是否能回滚；
7. 是否影响荣誉；
8. 是否需要消息；
9. 是否需要审计；
10. 是否会泄露未成年人信息。

---

# 一百二十四、最终技术原则

> **身份在云端确认。**

> **权限在云端确认。**

> **业务状态在云端确认。**

> **永久ID由云端维护。**

> **第X位小读者由事务生成。**

> **荣誉只通过流水改变。**

> **正常物流批量处理。**

> **异常物流才进入永久图书ID。**

> **重要数据不物理删除。**

> **关键修改全部留痕。**

> **前端永远不能成为业务规则的最终裁判。**
